CREATE TABLE GLUMAC
(
    JMBG NUMBER(13) PRIMARY KEY,
    IME VARCHAR2(15) NOT NULL,
    PREZIME VARCHAR2(15) NOT NULL,
    DATUM_RODJENJA DATE DEFAULT SYSDATE NOT NULL,
    ID_TRUPE NUMBER(9),
    CONSTRAINT GLUMAC_ID_TRUPE_FK FOREIGN 
    KEY(ID_TRUPE) REFERENCES GLUMAC(JMBG)

);